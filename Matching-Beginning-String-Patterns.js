// Use the caret character in a regex to find "Cal" only in the beginning of the string rickyAndCal.

let rickAndCal = "Cal and Rick both like racing.";
let calRegex = /^Cal/;
let result = calRegex.testcrickAndCal);
